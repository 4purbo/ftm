import './Intro.css'
import AppBar from "./AppBar";

const Intro = () => {
    return (
        <section className="intro">
            <AppBar></AppBar>
        </section>
    );
}

export default Intro;